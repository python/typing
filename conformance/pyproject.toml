[project]
name = "typing-conformance"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "jinja2",
    "mypy",
    "pycroscope",
    "pyrefly",
    "pyright",
    "tomlkit",
    "ty",
    "zuban",
]

[tool.uv]
package = false
